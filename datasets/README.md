# datasets/

`adapters/` - one adapter per licensed dataset (none yet), producing a common record format.
`manifests/` - hashes/inventories of *local* licensed copies for reproducibility.
`data/` - gitignored; licensed data lives here on your machine only.

See docs/datasets.md for the inventory and licensing constraints.
